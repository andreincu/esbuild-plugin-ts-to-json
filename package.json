{
  "name": "esbuild-plugin-ts-to-json",
  "version": "1.0.1",
  "description": "A plugin for esbuild to transform TypeScript files to JSON format. Specific for generating manifest.json.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.cjs",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "node --no-warnings --test \"test/**/*.test.js\"",
    "tag": "git tag v$(node -p \"require('./package.json').version\") && git push --tags",
    "esbuild": "node esbuildrc.js"
  },
  "keywords": [
    "esbuild",
    "plugin"
  ],
  "author": "Andrei Iancu",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.9.3",
    "esbuild": "^0.24.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "flugin-manifest-config": "github:andreincu/flugin-manifest-config#v1.0.0"
  },
  "peerDependencies": {
    "esbuild": "^0.24.0"
  }
}
